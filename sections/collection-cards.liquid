{{ 'section-collection-cards.css' | asset_url | stylesheet_tag }}
<script src="{{ 'section-collection-cards.js' | asset_url }}" defer></script>

{%- liquid
  assign layout_mode = section.settings.layout_mode | default: 'carousel'
  assign section_width_mode = section.settings.section_width | default: 'full'
  if section_width_mode contains 'site'
    assign section_width_mode = 'site'
  elsif section_width_mode contains 'custom'
    assign section_width_mode = 'custom'
  elsif section_width_mode contains 'full'
    assign section_width_mode = 'full'
  endif
  unless section_width_mode == 'site' or section_width_mode == 'full' or section_width_mode == 'custom'
    assign section_width_mode = 'full'
  endunless
  assign custom_section_width = section.settings.custom_section_width | default: 1500
  assign card_ratio = section.settings.card_image_ratio | default: '4-5'
  assign show_arrows = section.settings.show_arrows
  assign show_count_default = section.settings.show_count_default
  assign use_first_product_image = section.settings.use_first_product_image

  assign section_width_value = '100%'
  if section_width_mode == 'site'
    assign section_width_value = 'var(--page-width)'
  elsif section_width_mode == 'custom'
    capture section_width_value
      echo custom_section_width
      echo 'px'
    endcapture
  endif

  assign ratio_value = '4 / 5'
  case card_ratio
    when '1-1'
      assign ratio_value = '1 / 1'
    when '4-3'
      assign ratio_value = '4 / 3'
    when '16-9'
      assign ratio_value = '16 / 9'
  endcase
-%}

<section
  class="cc-carousel cc-carousel--layout-{{ layout_mode }} cc-carousel--width-{{ section_width_mode }} cc-carousel--ratio-{{ card_ratio }}{% if section.settings.border_top %} cc-carousel--border-top{% endif %}"
  aria-label="{{ section.settings.heading | default: 'Collection cards' | escape }}"
  style="
    --cc-container-width: {{ section_width_value }};
    --cc-custom-width: {{ custom_section_width }}px;
    --cc-bg: {{ section.settings.background_color }};
    --cc-border-color: {{ section.settings.border_color }};
    --cc-gap-desktop: {{ section.settings.gap_desktop }}px;
    --cc-gap-mobile: {{ section.settings.gap_mobile }}px;
    --cc-image-ratio: {{ ratio_value }};
    --cc-title-size-desktop: {{ section.settings.card_title_size_desktop }}px;
    --cc-title-size-mobile: {{ section.settings.card_title_size_mobile }}px;
    --cc-title-weight: {{ section.settings.card_title_weight }};
    --cc-title-line-height: {{ section.settings.card_title_line_height | divided_by: 100.0 }};
    --cc-title-letter-spacing: {{ section.settings.card_title_letter_spacing | divided_by: 100.0 }}em;
    --cc-title-transform: {{ section.settings.card_title_transform }};
    --cc-title-color: {{ section.settings.card_title_color }};
    --cc-meta-size-desktop: {{ section.settings.card_meta_size_desktop }}px;
    --cc-meta-size-mobile: {{ section.settings.card_meta_size_mobile }}px;
    --cc-meta-weight: {{ section.settings.card_meta_weight }};
    --cc-meta-line-height: {{ section.settings.card_meta_line_height | divided_by: 100.0 }};
    --cc-meta-letter-spacing: {{ section.settings.card_meta_letter_spacing | divided_by: 100.0 }}em;
    --cc-meta-transform: {{ section.settings.card_meta_transform }};
    --cc-meta-color: {{ section.settings.card_meta_color }};
    --cc-padding-top-desktop: {{ section.settings.padding_top_desktop }}px;
    --cc-padding-bottom-desktop: {{ section.settings.padding_bottom_desktop }}px;
    --cc-padding-left-desktop: {{ section.settings.padding_left_desktop }}px;
    --cc-padding-right-desktop: {{ section.settings.padding_right_desktop }}px;
    --cc-padding-top-mobile: {{ section.settings.padding_top_mobile }}px;
    --cc-padding-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;
    --cc-padding-left-mobile: {{ section.settings.padding_left_mobile }}px;
    --cc-padding-right-mobile: {{ section.settings.padding_right_mobile }}px;
    --cc-margin-top-desktop: {{ section.settings.margin_top_desktop }}px;
    --cc-margin-bottom-desktop: {{ section.settings.margin_bottom_desktop }}px;
    --cc-margin-top-mobile: {{ section.settings.margin_top_mobile }}px;
    --cc-margin-bottom-mobile: {{ section.settings.margin_bottom_mobile }}px;
  "
  {{ section.shopify_attributes }}
>
  <div class="cc-carousel__inner">
    <div class="cc-carousel__head">
      <div class="cc-carousel__heading-group">
        <h2 class="cc-carousel__title">{{ section.settings.heading }}</h2>
        {%- if section.settings.heading_subtitle != blank -%}
          <p class="cc-carousel__subtitle">{{ section.settings.heading_subtitle }}</p>
        {%- endif -%}
      </div>
      <div class="cc-carousel__nav{% unless layout_mode == 'carousel' and show_arrows %} is-hidden{% endunless %}">
        {%- if layout_mode == 'carousel' and show_arrows -%}
          <button type="button" class="cc-carousel__btn" data-cc-prev aria-label="Previous collection cards">
            {% render 'icon-yaomri-chevron-left' %}
          </button>
          <button type="button" class="cc-carousel__btn" data-cc-next aria-label="Next collection cards">
            {% render 'icon-yaomri-chevron-right' %}
          </button>
        {%- endif -%}
      </div>
    </div>

    {%- assign valid_cards = 0 -%}
    {%- for block in section.blocks -%}
      {%- assign coll = collections[block.settings.collection] -%}
      {%- assign fallback_product_image = blank -%}
      {%- if use_first_product_image and coll != blank and coll.products_count > 0 and coll.products.first.featured_image != blank -%}
        {%- assign fallback_product_image = coll.products.first.featured_image -%}
      {%- endif -%}
      {%- assign card_title_check = block.settings.title | default: coll.title -%}
      {%- if card_title_check != blank or block.settings.image != blank or coll.image != blank or fallback_product_image != blank -%}
        {%- assign valid_cards = valid_cards | plus: 1 -%}
      {%- endif -%}
    {%- endfor -%}

    {%- if valid_cards > 0 or request.design_mode -%}
      <div
        class="cc-carousel__track{% if layout_mode == 'grid' %} cc-carousel__track--grid{% endif %}"
        data-cc-track
      >
        {%- for block in section.blocks -%}
          {%- liquid
            assign coll = collections[block.settings.collection]
            assign fallback_product_image = blank
            if use_first_product_image and coll != blank and coll.products_count > 0 and coll.products.first.featured_image != blank
              assign fallback_product_image = coll.products.first.featured_image
            endif
            assign card_img = block.settings.image | default: coll.image | default: fallback_product_image
            assign card_title = block.settings.title | default: coll.title
            assign card_subtitle = block.settings.subtitle
            assign card_link = block.settings.link_override | default: coll.url
            assign show_count = block.settings.show_count
            if show_count == blank
              assign show_count = show_count_default
            endif
            assign has_content = false
            if card_title != blank or card_img != blank
              assign has_content = true
            endif
          -%}
          {%- if has_content -%}
            {%- if card_link != blank and valid_cards > 0 -%}
              <a
                href="{{ card_link }}"
                class="cc-card cc-card--hover-{{ section.settings.image_hover }}"
                style="--cc-image-position: {{ block.settings.image_position | default: 'center center' }};"
                {{ block.shopify_attributes }}
                {% if block.settings.open_new_window %}target="_blank" rel="noopener noreferrer"{% endif %}
                aria-label="{{ card_title | escape }}"
              >
                <div class="cc-card__media">
                  {%- if block.settings.badge_text != blank -%}
                    <span
                      class="cc-card__badge cc-card__badge--{{ block.settings.badge_placement }}"
                      style="
                        --cc-badge-text: {{ block.settings.badge_text_color }};
                        --cc-badge-bg: {{ block.settings.badge_background_color }};
                        --cc-badge-size: {{ block.settings.badge_font_size }}px;
                        --cc-badge-transform: {{ block.settings.badge_text_transform }};
                      "
                    >
                      {{ block.settings.badge_text }}
                    </span>
                  {%- endif -%}
                  {%- if card_img != blank -%}
                    {{
                      card_img
                      | image_url: width: 900
                      | image_tag:
                        loading: 'lazy',
                        widths: '300,500,700,900',
                        sizes: '(min-width:1024px) 24vw, (min-width:768px) 32vw, (min-width:640px) 45vw, 70vw',
                        class: 'cc-card__img',
                        alt: card_title | default: card_img.alt | escape
                    }}
                  {%- else -%}
                    <div class="cc-card__placeholder">{{ 'collection-1' | placeholder_svg_tag }}</div>
                  {%- endif -%}
                  <div class="cc-card__overlay">
                    {%- if card_title != blank -%}
                      <p class="cc-card__title">{{ card_title }}</p>
                    {%- endif -%}
                    {%- if show_count and coll.products_count > 0 -%}
                      <p class="cc-card__count">{{ coll.products_count }} pieces</p>
                    {%- elsif card_subtitle != blank -%}
                      <p class="cc-card__count">{{ card_subtitle }}</p>
                    {%- endif -%}
                  </div>
                </div>
              </a>
            {%- else -%}
              <article
                class="cc-card cc-card--hover-{{ section.settings.image_hover }}"
                style="--cc-image-position: {{ block.settings.image_position | default: 'center center' }};"
                {{ block.shopify_attributes }}
              >
                <div class="cc-card__media">
                  {%- if block.settings.badge_text != blank -%}
                    <span
                      class="cc-card__badge cc-card__badge--{{ block.settings.badge_placement }}"
                      style="
                        --cc-badge-text: {{ block.settings.badge_text_color }};
                        --cc-badge-bg: {{ block.settings.badge_background_color }};
                        --cc-badge-size: {{ block.settings.badge_font_size }}px;
                        --cc-badge-transform: {{ block.settings.badge_text_transform }};
                      "
                    >
                      {{ block.settings.badge_text }}
                    </span>
                  {%- endif -%}
                  {%- if card_img != blank -%}
                    {{
                      card_img
                      | image_url: width: 900
                      | image_tag:
                        loading: 'lazy',
                        widths: '300,500,700,900',
                        sizes: '(min-width:1024px) 24vw, (min-width:768px) 32vw, (min-width:640px) 45vw, 70vw',
                        class: 'cc-card__img',
                        alt: card_title | default: card_img.alt | escape
                    }}
                  {%- else -%}
                    <div class="cc-card__placeholder">{{ 'collection-1' | placeholder_svg_tag }}</div>
                  {%- endif -%}
                  <div class="cc-card__overlay">
                    {%- if card_title != blank -%}
                      <p class="cc-card__title">{{ card_title }}</p>
                    {%- endif -%}
                    {%- if show_count and coll.products_count > 0 -%}
                      <p class="cc-card__count">{{ coll.products_count }} pieces</p>
                    {%- elsif card_subtitle != blank -%}
                      <p class="cc-card__count">{{ card_subtitle }}</p>
                    {%- endif -%}
                  </div>
                </div>
              </article>
            {%- endif -%}
          {%- elsif request.design_mode -%}
            <article class="cc-card cc-card--placeholder" {{ block.shopify_attributes }}>
              <div class="cc-card__media">
                <div class="cc-card__placeholder">{{ 'collection-1' | placeholder_svg_tag }}</div>
              </div>
              <div class="cc-card__overlay">
                <p class="cc-card__title">Collection</p>
              </div>
            </article>
          {%- endif -%}
        {%- endfor -%}
        {%- if request.design_mode and valid_cards == 0 and section.blocks.size == 0 -%}
          {%- for i in (1..4) -%}
            <article class="cc-card cc-card--placeholder">
              <div class="cc-card__media">
                <div class="cc-card__placeholder">{{ 'collection-1' | placeholder_svg_tag }}</div>
              </div>
              <div class="cc-card__overlay">
                <p class="cc-card__title">Collection</p>
              </div>
            </article>
          {%- endfor -%}
        {%- endif -%}
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Collection cards",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Shop by collection" },
    { "type": "text", "id": "heading_subtitle", "label": "Heading subtitle", "default": "Curated edits" },
    { "type": "checkbox", "id": "show_arrows", "label": "Show arrows", "default": true },
    {
      "type": "select",
      "id": "layout_mode",
      "label": "Layout",
      "default": "carousel",
      "options": [
        { "value": "carousel", "label": "Carousel" },
        { "value": "grid", "label": "Grid" }
      ]
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "default": "full",
      "options": [
        { "value": "site", "label": "Site width" },
        { "value": "full", "label": "Full width" },
        { "value": "custom", "label": "Custom width" }
      ]
    },
    { "type": "range", "id": "custom_section_width", "label": "Custom section width", "min": 720, "max": 2200, "step": 20, "unit": "px", "default": 1500 },
    { "type": "color", "id": "background_color", "label": "Background colour", "default": "#ffffff" },
    { "type": "checkbox", "id": "border_top", "label": "Border top", "default": true },
    { "type": "color", "id": "border_color", "label": "Border colour", "default": "#e8e8e8" },
    {
      "type": "select",
      "id": "card_image_ratio",
      "label": "Card image ratio",
      "default": "4-5",
      "options": [
        { "value": "4-5", "label": "Portrait 4:5" },
        { "value": "1-1", "label": "Square 1:1" },
        { "value": "4-3", "label": "Landscape 4:3" },
        { "value": "16-9", "label": "Wide 16:9" }
      ]
    },
    {
      "type": "select",
      "id": "image_hover",
      "label": "Image hover",
      "default": "subtle_zoom",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "subtle_zoom", "label": "Subtle zoom" }
      ]
    },
    { "type": "range", "id": "gap_desktop", "label": "Gap desktop", "min": 0, "max": 40, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "gap_mobile", "label": "Gap mobile", "min": 0, "max": 28, "step": 1, "unit": "px", "default": 12 },
    { "type": "checkbox", "id": "show_count_default", "label": "Show product count by default", "default": true },
    {
      "type": "checkbox",
      "id": "use_first_product_image",
      "label": "Use first product image when collection image is missing",
      "default": true,
      "info": "Collection image is used first. If unavailable, the first product image can be used as a fallback."
    },
    { "type": "header", "content": "Card title typography" },
    { "type": "range", "id": "card_title_size_desktop", "label": "Card title size desktop", "min": 12, "max": 32, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "card_title_size_mobile", "label": "Card title size mobile", "min": 11, "max": 24, "step": 1, "unit": "px", "default": 14 },
    { "type": "select", "id": "card_title_weight", "label": "Card title weight", "default": "800", "options": [{ "value": "300", "label": "Light" }, { "value": "400", "label": "Regular" }, { "value": "500", "label": "Medium" }, { "value": "600", "label": "Semibold" }, { "value": "700", "label": "Bold" }, { "value": "800", "label": "Extra bold" }] },
    { "type": "range", "id": "card_title_line_height", "label": "Card title line height", "min": 80, "max": 180, "step": 5, "unit": "%", "default": 120 },
    { "type": "range", "id": "card_title_letter_spacing", "label": "Card title letter spacing", "min": -10, "max": 40, "step": 1, "unit": "em", "default": 0 },
    { "type": "select", "id": "card_title_transform", "label": "Card title text transform", "default": "uppercase", "options": [{ "value": "none", "label": "None" }, { "value": "uppercase", "label": "Uppercase" }, { "value": "lowercase", "label": "Lowercase" }, { "value": "capitalize", "label": "Capitalize" }] },
    { "type": "color", "id": "card_title_color", "label": "Card title colour", "default": "#ffffff" },
    { "type": "header", "content": "Card meta typography" },
    { "type": "range", "id": "card_meta_size_desktop", "label": "Card meta size desktop", "min": 10, "max": 24, "step": 1, "unit": "px", "default": 11 },
    { "type": "range", "id": "card_meta_size_mobile", "label": "Card meta size mobile", "min": 10, "max": 20, "step": 1, "unit": "px", "default": 10 },
    { "type": "select", "id": "card_meta_weight", "label": "Card meta weight", "default": "600", "options": [{ "value": "300", "label": "Light" }, { "value": "400", "label": "Regular" }, { "value": "500", "label": "Medium" }, { "value": "600", "label": "Semibold" }, { "value": "700", "label": "Bold" }, { "value": "800", "label": "Extra bold" }] },
    { "type": "range", "id": "card_meta_line_height", "label": "Card meta line height", "min": 80, "max": 220, "step": 5, "unit": "%", "default": 130 },
    { "type": "range", "id": "card_meta_letter_spacing", "label": "Card meta letter spacing", "min": -10, "max": 40, "step": 1, "unit": "em", "default": 18 },
    { "type": "select", "id": "card_meta_transform", "label": "Card meta text transform", "default": "uppercase", "options": [{ "value": "none", "label": "None" }, { "value": "uppercase", "label": "Uppercase" }, { "value": "lowercase", "label": "Lowercase" }, { "value": "capitalize", "label": "Capitalize" }] },
    { "type": "color", "id": "card_meta_color", "label": "Card meta colour", "default": "#ffffff" },

    { "type": "header", "content": "Spacing" },
    { "type": "range", "id": "padding_top_desktop", "label": "Padding top desktop", "min": 0, "max": 120, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_bottom_desktop", "label": "Padding bottom desktop", "min": 0, "max": 120, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_left_desktop", "label": "Padding left desktop", "min": 0, "max": 120, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_right_desktop", "label": "Padding right desktop", "min": 0, "max": 120, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_top_mobile", "label": "Padding top mobile", "min": 0, "max": 100, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_bottom_mobile", "label": "Padding bottom mobile", "min": 0, "max": 100, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_left_mobile", "label": "Padding left mobile", "min": 0, "max": 80, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_right_mobile", "label": "Padding right mobile", "min": 0, "max": 80, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "margin_top_desktop", "label": "Margin top desktop", "min": 0, "max": 120, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "margin_bottom_desktop", "label": "Margin bottom desktop", "min": 0, "max": 120, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "margin_top_mobile", "label": "Margin top mobile", "min": 0, "max": 100, "step": 2, "unit": "px", "default": 0 },
    { "type": "range", "id": "margin_bottom_mobile", "label": "Margin bottom mobile", "min": 0, "max": 100, "step": 2, "unit": "px", "default": 0 }
  ],
  "blocks": [
    {
      "type": "collection",
      "name": "Collection card",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection",
          "info": "Uses collection title, link, and image by default."
        },
        { "type": "text", "id": "title", "label": "Card title override" },
        { "type": "text", "id": "subtitle", "label": "Subtitle override" },
        { "type": "image_picker", "id": "image", "label": "Image override", "info": "Overrides the collection image." },
        { "type": "checkbox", "id": "show_count", "label": "Show product count", "default": true },
        { "type": "url", "id": "link_override", "label": "Link override" },
        { "type": "text", "id": "badge_text", "label": "Badge text" },
        {
          "type": "select",
          "id": "badge_placement",
          "label": "Badge placement",
          "default": "top-left",
          "options": [
            { "value": "top-left", "label": "Top left" },
            { "value": "top-right", "label": "Top right" },
            { "value": "bottom-left", "label": "Bottom left" },
            { "value": "bottom-right", "label": "Bottom right" }
          ]
        },
        { "type": "color", "id": "badge_text_color", "label": "Badge text colour", "default": "#ffffff" },
        { "type": "color", "id": "badge_background_color", "label": "Badge background colour", "default": "#111111" },
        { "type": "range", "id": "badge_font_size", "label": "Badge font size", "min": 10, "max": 18, "step": 1, "unit": "px", "default": 11 },
        {
          "type": "select",
          "id": "badge_text_transform",
          "label": "Badge text transform",
          "default": "uppercase",
          "options": [
            { "value": "none", "label": "None" },
            { "value": "uppercase", "label": "Uppercase" },
            { "value": "lowercase", "label": "Lowercase" },
            { "value": "capitalize", "label": "Capitalize" }
          ]
        },
        {
          "type": "select",
          "id": "image_position",
          "label": "Image position",
          "default": "center center",
          "options": [
            { "value": "center center", "label": "Centre" },
            { "value": "center top", "label": "Top" },
            { "value": "center bottom", "label": "Bottom" },
            { "value": "left center", "label": "Left" },
            { "value": "right center", "label": "Right" }
          ]
        },
        { "type": "checkbox", "id": "open_new_window", "label": "Open in new tab", "default": false }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [
    {
      "name": "Collection cards",
      "blocks": [
        { "type": "collection" },
        { "type": "collection" },
        { "type": "collection" },
        { "type": "collection" }
      ]
    }
  ]
}
{% endschema %}
