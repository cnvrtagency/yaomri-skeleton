<footer class="site-footer cnvrt-footer">
  <div class="footer__copyright cnvrt-footer__copyright">
    &copy;
    {{ 'now' | date: '%Y' }}
    {{ shop.name | link_to: routes.root_url }}, {{ powered_by_link }}
  </div>

  <div class="footer__links cnvrt-footer__links">
    {% for link in section.settings.menu.links %}
      {{ link.title | link_to: link.url }}
    {% endfor %}
  </div>

  <div class="footer__payment cnvrt-footer__payment">
    {% if section.settings.show_payment_icons %}
      {% for type in shop.enabled_payment_types %}
        {{ type | payment_type_svg_tag }}
      {% endfor %}
    {% endif %}
  </div>
</footer>

{% stylesheet %}
  .site-footer,
  .cnvrt-footer {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    align-items: center;
    margin-top: 2rem;
    margin-inline: auto;
    max-width: min(var(--page-width), 100%);
    padding-inline: 0;
    gap: 0.75rem 1rem;
  }
  .site-footer a,
  .cnvrt-footer a {
    text-decoration: none;
    color: var(--color-foreground);
  }
  .site-footer .footer__links,
  .site-footer .footer__payment,
  .cnvrt-footer .cnvrt-footer__links,
  .cnvrt-footer .cnvrt-footer__payment {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  @media (max-width: 749px) {
    .site-footer,
    .cnvrt-footer {
      padding-inline: 0;
      margin-top: 1.5rem;
      gap: 0.6rem 0.85rem;
    }

    .site-footer .footer__links,
    .site-footer .footer__payment,
    .cnvrt-footer .cnvrt-footer__links,
    .cnvrt-footer .cnvrt-footer__payment {
      gap: 0.75rem;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.footer",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:labels.menu"
    },
    {
      "type": "checkbox",
      "id": "show_payment_icons",
      "label": "t:labels.show_payment_icons",
      "default": true
    }
  ]
}
{% endschema %}
