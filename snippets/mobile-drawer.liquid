{%- liquid
  assign drawer_menu = menu

  assign drawer_title = section.settings.mobile_menu_label
  assign has_drawer_title = false
  if drawer_title != blank
    assign has_drawer_title = true
  endif
  assign search_placeholder = section.settings.mobile_drawer_search_placeholder | default: 'Search Ya Omri'
-%}

<div
  id="MobileDrawer"
  class="mdrawer"
  data-mdrawer
  aria-hidden="true"
  style="
    --paper: {{ section.settings.mobile_drawer_background }};
    --ink: {{ section.settings.mobile_drawer_text }};
    --mdrawer-max-width: {{ section.settings.mobile_drawer_width }}px;
  "
>
  <button class="mdrawer__scrim" type="button" data-mdrawer-close aria-label="Close menu"></button>

  <aside class="mdrawer__panel" role="dialog" aria-modal="true"{% if has_drawer_title %} aria-labelledby="MobileDrawerTitle"{% else %} aria-label="Mobile menu"{% endif %} tabindex="-1">
    <header class="mdrawer__header is-root{% unless has_drawer_title %} mdrawer__header--no-title{% endunless %}" data-mdrawer-header>
      <button class="mdrawer__icon-btn mdrawer__back" type="button" data-mdrawer-back hidden aria-label="Back">
        {% render 'icon-yaomri-chevron-left' %}
      </button>
      {%- if has_drawer_title -%}
        <h2 id="MobileDrawerTitle" class="mdrawer__title" data-mdrawer-title>{{ drawer_title }}</h2>
      {%- endif -%}
      <button class="mdrawer__icon-btn mdrawer__close" type="button" data-mdrawer-close aria-label="Close menu">
        {% render 'icon-yaomri-close' %}
      </button>
    </header>

    <div class="mdrawer__body">
      <div class="mdrawer__view is-active" data-mdrawer-view="root">
        {%- if section.settings.show_mobile_drawer_search -%}
          <form class="mdrawer__search" action="{{ routes.search_url }}" method="get" role="search">
            <label class="visually-hidden" for="DrawerSearchInput">Search</label>
            <span class="mdrawer__search-icon" aria-hidden="true">{% render 'icon-yaomri-search' %}</span>
            <input id="DrawerSearchInput" type="search" name="q" placeholder="{{ search_placeholder | escape }}" autocomplete="off">
            <input type="hidden" name="options[prefix]" value="last">
          </form>
        {%- endif -%}

        <nav class="mdrawer__nav" aria-label="Mobile menu">
          <ul class="mdrawer__nav-list" role="list">
            {%- for link in drawer_menu.links -%}
              <li>
                {%- if link.links.size > 0 -%}
                  {%- assign panel_id = 'panel-' | append: link.handle -%}
                  <button
                    class="mdrawer__nav-item"
                    type="button"
                    data-mdrawer-drill
                    data-target="{{ panel_id }}"
                    data-controls="drawer-panel-{{ panel_id }}"
                    aria-controls="drawer-panel-{{ panel_id }}"
                    aria-expanded="false"
                    data-title="{{ link.title | escape }}"
                  >
                    <span>{{ link.title }}</span>
                    <span class="mdrawer__chevron" aria-hidden="true">{% render 'icon-yaomri-chevron-right' %}</span>
                  </button>
                {%- else -%}
                  <a class="mdrawer__nav-item" href="{{ link.url }}">{{ link.title }}</a>
                {%- endif -%}
              </li>
            {%- endfor -%}
          </ul>
        </nav>

        {%- if section.settings.show_mobile_drawer_footer and section.blocks.size > 0 -%}
          {%- capture drawer_footer_content -%}
            {%- for block in section.blocks -%}
              {%- case block.type -%}
                {%- when 'footer_link' -%}
                  {%- liquid
                    assign footer_link = block.settings.link
                    assign footer_label = block.settings.label
                    assign footer_icon = block.settings.icon | default: 'none'
                    if footer_icon == 'account' and footer_link == blank
                      if shop.customer_accounts_enabled
                        assign footer_link = routes.account_login_url
                      endif
                    elsif footer_icon == 'heart' and footer_link == blank
                      assign footer_link = settings.wishlist_link | default: wishlist_url | default: '/pages/wishlist'
                    endif
                  -%}
                  {%- if footer_label != blank and footer_link != blank -%}
                    <a class="mdrawer__footer-item" href="{{ footer_link }}" {% if block.settings.open_new_window %}target="_blank" rel="noopener"{% endif %} {{ block.shopify_attributes }}>
                      {%- if footer_icon == 'account' and shop.customer_accounts_enabled -%}
                        <span class="mdrawer__footer-icon" aria-hidden="true">{% render 'icon-yaomri-account' %}</span>
                      {%- elsif footer_icon == 'heart' -%}
                        <span class="mdrawer__footer-icon" aria-hidden="true">{% render 'icon-yaomri-heart' %}</span>
                      {%- endif -%}
                      <span>{{ footer_label }}</span>
                    </a>
                  {%- endif -%}
                {%- when 'country_selector' -%}
                  {%- if localization.available_countries.size > 1 -%}
                    {%- form 'localization', id: 'MobileDrawerCountryForm', class: 'mdrawer__country-form' -%}
                      {%- if block.settings.label != blank -%}
                        <label class="mdrawer__country-label" for="MobileDrawerCountrySelector">{{ block.settings.label }}</label>
                      {%- else -%}
                        <label class="visually-hidden" for="MobileDrawerCountrySelector">Country/region</label>
                      {%- endif -%}
                      <select
                        id="MobileDrawerCountrySelector"
                        class="mdrawer__country-select"
                        name="country_code"
                        aria-label="Country/region"
                        onchange="this.form.submit()"
                      >
                        {%- for country in localization.available_countries -%}
                          <option
                            value="{{ country.iso_code }}"
                            {%- if country.iso_code == localization.country.iso_code -%} selected{%- endif -%}
                          >
                            {{ country.name }} ({{ country.currency.iso_code }}{% if country.currency.symbol != blank %} {{ country.currency.symbol }}{% endif %})
                          </option>
                        {%- endfor -%}
                      </select>
                    {%- endform -%}
                  {%- endif -%}
                {%- when 'text_row' -%}
                  {%- if block.settings.label != blank or block.settings.text != blank -%}
                    <p class="mdrawer__footer-text" {{ block.shopify_attributes }}>
                      {%- if block.settings.label != blank -%}<span>{{ block.settings.label }}</span>{%- endif -%}
                      {%- if block.settings.text != blank -%}<strong>{{ block.settings.text }}</strong>{%- endif -%}
                    </p>
                  {%- endif -%}
                {%- when 'social_link' -%}
                  {%- if block.settings.label != blank and block.settings.link != blank -%}
                    <a class="mdrawer__footer-item mdrawer__footer-item--social" href="{{ block.settings.link }}" {% if block.settings.open_new_window %}target="_blank" rel="noopener"{% endif %} {{ block.shopify_attributes }}>
                      <span>{{ block.settings.label }}</span>
                    </a>
                  {%- endif -%}
              {%- endcase -%}
            {%- endfor -%}
          {%- endcapture -%}
          {%- assign drawer_footer_content = drawer_footer_content | strip -%}
          {%- if drawer_footer_content != blank -%}
            <div class="mdrawer__footer" data-mdrawer-root-only>
              {{ drawer_footer_content }}
            </div>
          {%- endif -%}
        {%- endif -%}
      </div>

      {%- for link in drawer_menu.links -%}
        {%- if link.links.size > 0 -%}
          {%- assign panel_id = 'panel-' | append: link.handle -%}
          <div class="mdrawer__view" data-mdrawer-view="{{ panel_id }}" id="drawer-panel-{{ panel_id }}">
            <nav class="mdrawer__drill-nav" aria-label="{{ link.title | escape }}">
              <ul class="mdrawer__drill-list" role="list">
                {%- assign ungrouped_started = false -%}
                {%- for child in link.links -%}
                  {%- assign parts = child.title | split: '/' -%}
                  {%- assign group_name = '' -%}
                  {%- assign item_label = child.title -%}
                  {%- if parts.size > 1 -%}
                    {%- assign group_name = parts[0] | strip -%}
                    {%- assign item_label = parts[1] | strip -%}
                  {%- endif -%}

                  {%- if group_name != blank -%}
                    {%- ifchanged group_name -%}
                      <li class="mdrawer__group">{{ group_name }}</li>
                    {%- endifchanged -%}
                  {%- elsif ungrouped_started == false -%}
                    <li class="mdrawer__group">Shop</li>
                    {%- assign ungrouped_started = true -%}
                  {%- endif -%}

                  <li>
                    <a class="mdrawer__drill-item" href="{{ child.url }}">
                      {{ item_label }}
                    </a>
                  </li>
                {%- endfor -%}
              </ul>
            </nav>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </aside>
</div>
